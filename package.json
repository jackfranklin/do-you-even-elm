{
  "name": "do-you-even-elm",
  "version": "1.1.0",
  "description": "",
  "private": true,
  "scripts": {
    "start": "elm-app start",
    "build": "elm-app build",
    "test": "elm-app test",
    "deploy": "elm-app build && surge --domain doyouevenelm.com --project build",
    "install-all": "yarn install && elm-package install --yes && elm-test --add-dependencies tests/elm-package.json && (cd tests && elm-package install --yes)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "create-elm-app": "^1.6.0",
    "elm": "^0.18.0",
    "elm-test": "^0.18.9",
    "surge": "^0.19.0"
  }
}
